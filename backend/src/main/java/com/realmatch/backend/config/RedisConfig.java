package com.realmatch.backend.config;

import org.springframework.context.annotation.Configuration;

/** Redis 설정 랜덤통화 대기열, 채팅 라우팅, 결제 중복 방지 캐시에 사용할 Redis 설정을 관리합니다. */
@Configuration
public class RedisConfig {

  // TODO: RedisTemplate, key serializer, cache manager, pub/sub listener 설정을 구현합니다.
}
